kind = "web"
previewPath = "/"
title = "GroundCheck"
version = "1.0.0"
id = "artifacts/groundcheck"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24828

[services.development]
run = "pnpm --filter @workspace/groundcheck run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/groundcheck", "run", "build" ]
serve = "static"
publicDir = "artifacts/groundcheck/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24828"
BASE_PATH = "/"
